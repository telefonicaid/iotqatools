language: python
python:
  - "2.7"
# command to install dependencies
install:
  - python setup.py install
#  - pip install -r requirements.txt
# command to run tests
script:
#python setup.py test
  - python iotqatools/tests/cb_utils_tests.py

branches:
  only:
    - develop
    - master
